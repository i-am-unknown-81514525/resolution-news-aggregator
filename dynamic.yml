http:
  routers:
    backend-router:
      rule: "PathPrefix(`/api`) || PathPrefix(`/ws`)"
      service: backend-service
      entryPoints:
        - web

  services:
    backend-service:
      loadBalancer:
        servers:
          - url: "http://backend:3000"